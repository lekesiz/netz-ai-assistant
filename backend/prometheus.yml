global:
  scrape_interval: 15s
  evaluation_interval: 15s
  external_labels:
    monitor: 'netz-ai'
    environment: 'production'

# Alerting configuration
alerting:
  alertmanagers:
    - static_configs:
        - targets: []
          # - 'alertmanager:9093'

# Load rules once and periodically evaluate them
rule_files:
  - '/etc/prometheus/alerts/*.yml'

# Scrape configurations
scrape_configs:
  # NETZ AI API metrics
  - job_name: 'netz-api'
    static_configs:
      - targets: ['netz-api:9090']
        labels:
          service: 'netz-ai-api'
          env: 'production'
    scrape_interval: 10s
    metrics_path: '/metrics'

  # Node exporter for system metrics (optional)
  - job_name: 'node-exporter'
    static_configs:
      - targets: ['node-exporter:9100']
        labels:
          service: 'node-exporter'

  # PostgreSQL exporter (optional)
  - job_name: 'postgres'
    static_configs:
      - targets: ['postgres-exporter:9187']
        labels:
          service: 'postgresql'

  # Redis exporter (optional)
  - job_name: 'redis'
    static_configs:
      - targets: ['redis-exporter:9121']
        labels:
          service: 'redis'

  # Nginx exporter (optional)
  - job_name: 'nginx'
    static_configs:
      - targets: ['nginx-exporter:9113']
        labels:
          service: 'nginx'

# Alert rules example (save in alerts/netz_ai.yml)
# groups:
#   - name: netz_ai_alerts
#     interval: 30s
#     rules:
#       - alert: HighErrorRate
#         expr: rate(netz_ai_errors_total[5m]) > 0.05
#         for: 5m
#         labels:
#           severity: warning
#         annotations:
#           summary: High error rate detected
#           description: "Error rate is {{ $value }} errors per second"
#       
#       - alert: HighResponseTime
#         expr: histogram_quantile(0.95, rate(netz_ai_request_duration_seconds_bucket[5m])) > 1
#         for: 5m
#         labels:
#           severity: warning
#         annotations:
#           summary: High API response time
#           description: "95th percentile response time is {{ $value }} seconds"
#       
#       - alert: HighMemoryUsage
#         expr: netz_ai_memory_usage_percent > 90
#         for: 5m
#         labels:
#           severity: critical
#         annotations:
#           summary: High memory usage
#           description: "Memory usage is {{ $value }}%"